// 21st Century Artisan · GUR site — single-page; minimal JS.
// (Previous multi-page navbar toggle / SoundCloud player / contact form are
// gone with the rest of the multi-section site.)

document.addEventListener('DOMContentLoaded', () => {
  // Smooth-scroll for header anchor links is already handled by CSS
  // `html { scroll-behavior: smooth }`; nothing else to wire here yet.
});
